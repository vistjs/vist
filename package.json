{
  "name": "root",
  "private": true,
  "description": "record and test the web",
  "keywords": [
    "rtweb",
    "regression",
    "web",
    "testing"
  ],
  "author": "letshare <brucecai2012@gmail.com>",
  "bugs": "https://github.com/letshare/rtweb/issues",
  "homepage": "https://github.com/letshare/rtweb#readme",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/letshare/rtweb"
  },
  "scripts": {
    "postinstall": "husky install",
    "dev": "lerna run dev --parallel",
    "build": "lerna run build --stream",
    "lint": "eslint 'packages/**/*.{js,ts}' --quiet --fix"
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix",
    "*.ts?(x)": [
      "prettier --parser=typescript --write",
      "eslint"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^13.2.1",
    "@commitlint/config-conventional": "^13.2.0",
    "husky": "^7.0.4",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1"
  },
  "workspaces": [
    "./packages/*"
  ],
  "engines": {
    "node": "^14.18.0 || >=16.0.0",
    "pnpm": ">=7"
  }
}
